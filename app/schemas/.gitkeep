# Room schema JSONs are written here by KSP during the build.
# Each `*.json` file represents one @Database(version = N) and must be
# committed alongside the corresponding Migration in Migrations.kt.
