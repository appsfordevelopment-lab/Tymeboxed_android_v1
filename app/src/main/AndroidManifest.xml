<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- =========================================================== -->
    <!-- Android has no Screen Time API, so the blocking engine uses -->
    <!-- a broad set of permissions. Each is surfaced in the in-app  -->
    <!-- Settings > Permissions card with a deep-link to grant it.    -->
    <!-- =========================================================== -->

    <!-- Required for the blocking engine (Phase 3) -->
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- SessionBlockerService uses specialUse (see service declaration below). -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <!-- Timer / scheduling precision — SCHEDULE_EXACT_ALARM requires user
         grant on API 31+ but is allowed for non-alarm/calendar apps.
         USE_EXACT_ALARM is restricted to alarm clock / calendar apps only. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <!-- Keep the accessibility service alive across Doze / app standby -->
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Restart an active session after device reboot -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- NFC strategies (Tyme Boxed Mode / Manual Start) -->
    <uses-permission android:name="android.permission.NFC" />
    <uses-feature
        android:name="android.hardware.nfc"
        android:required="false" />

    <!-- Haptics on button press -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Future: tag verification via backend (unused in Phase 1) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".TymeBoxedApplication"
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/Theme.TymeBoxed"
        android:usesCleartextTraffic="false">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.TymeBoxed.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- NFC NDEF tag discovery (Phase 4). Placed on MainActivity so   -->
            <!-- foreground-dispatch can route reads to the active screen.     -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Also capture URL NDEF records so scanning an unregistered tag doesn't open a browser. -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="http" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="https" />
            </intent-filter>
            <!-- Explicit NDEF URL match for FoQos profile tags (some devices require host/path match). -->
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    tools:ignore="Typos"
                    android:scheme="https"
                    android:host="www.foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    tools:ignore="Typos"
                    android:scheme="https"
                    android:host="foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    tools:ignore="Typos"
                    android:scheme="http"
                    android:host="www.foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.nfc.action.NDEF_DISCOVERED" />
                <category android:name="android.intent.category.DEFAULT" />
                <data
                    tools:ignore="Typos"
                    android:scheme="http"
                    android:host="foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <!-- Some tags get routed as a direct VIEW deep-link; intercept the known FoQos URL too. -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    tools:ignore="Typos"
                    android:scheme="https"
                    android:host="www.foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    tools:ignore="Typos"
                    android:scheme="https"
                    android:host="foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    tools:ignore="Typos"
                    android:scheme="http"
                    android:host="www.foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data
                    tools:ignore="Typos"
                    android:scheme="http"
                    android:host="foqos.app"
                    android:pathPrefix="/profile/" />
            </intent-filter>
            <!-- Do NOT register ACTION_TAG_DISCOVERED: it matches every NFC tag (payment
                 cards, transit passes, etc.) and launches MainActivity when Reader Mode is
                 off (app in background). Tyme Boxed tags use NDEF (filters above); in-app
                 scans use Reader Mode in MainActivity / NfcIosStyleScanSheet. -->
        </activity>

        <!-- Blocking engine stubs (full implementation lands in Phase 3). -->
        <!-- Declared here so the manifest + DI graph line up with Phase 1. -->
        <!-- Focus-session blocking only; not an accessibility tool (Play policy). -->
        <service
            android:name=".service.AppBlockerAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:label="@string/app_name"
            tools:ignore="ExportedService">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <!-- Full-screen overlay when user opens something restricted during focus -->
        <activity
            android:name=".service.BlockerActivity"
            android:exported="false"
            android:excludeFromRecents="true"
            android:taskAffinity=""
            android:launchMode="singleTask"
            android:theme="@style/Theme.TymeBoxed" />

        <service
            android:name=".service.SessionBlockerService"
            android:exported="false"
            android:foregroundServiceType="specialUse">
            <property
                android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
                android:value="Displays the active focus session timer and stop action while a focus session is running." />
        </service>

        <!-- WorkManager is on the classpath but unused; strip its FGS so API 34+ type checks pass. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            tools:node="remove" />

        <receiver
            android:name=".service.BootCompletedReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.SessionReminderReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="dev.ambitionsoftware.tymeboxed.action.SESSION_END_REMINDER" />
            </intent-filter>
        </receiver>

        <receiver
            android:name=".service.ProfileScheduleReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="dev.ambitionsoftware.tymeboxed.action.SCHEDULE_START" />
                <action android:name="dev.ambitionsoftware.tymeboxed.action.SCHEDULE_END" />
            </intent-filter>
        </receiver>

        <!--
            Strict-mode uninstall protection (Brick-style).

            `android:label` + `android:description` are shown on the system
            "Activate device admin app?" screen, so the user understands *why*
            we're asking. Without them the dialog falls back to the class name
            and the OEM-rendered copy ("This admin can erase data, change screen
            lock…") which (a) scares the user and (b) overstates what we use.

            The full Brick-style chain of behaviours is documented in
            [TymeBoxedDeviceAdminReceiver] and [StrictModeProtection].
        -->
        <receiver
            android:name=".admin.TymeBoxedDeviceAdminReceiver"
            android:label="@string/device_admin_label"
            android:description="@string/device_admin_description"
            android:exported="true"
            android:permission="android.permission.BIND_DEVICE_ADMIN">
            <meta-data
                android:name="android.app.device_admin"
                android:resource="@xml/device_admin_receiver" />
            <intent-filter>
                <action android:name="android.app.action.DEVICE_ADMIN_ENABLED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLE_REQUESTED" />
                <action android:name="android.app.action.DEVICE_ADMIN_DISABLED" />
            </intent-filter>
        </receiver>
    </application>
</manifest>
