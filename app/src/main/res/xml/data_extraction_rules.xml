<?xml version="1.0" encoding="utf-8"?>
<!--
    Data extraction rules (API 31+).
    Exclude all data so user blocking profiles aren't mirrored off-device
    during Google Cloud Backup / Device-to-Device transfer until we have
    a sync story.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
